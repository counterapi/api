---
apiVersion: v1
kind: Service
metadata:
  name: counterapi-stable
spec:
  selector:
    app: counterapi
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: counterapi-preview
spec:
  selector:
    app: counterapi
  ports:
    - port: 80
      targetPort: 80
